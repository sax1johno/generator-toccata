version: "2.0"

services:
    # node-red
    node-red:
        extends:
            file: service-types.yml
            service: node-red
        build: ./
        restart: always
        ports:
            - '80:80'
        links:
            - views
    views:
        extends:
            file: service-types.yml
            service: microservice
        build: ./components/Views
        restart: always        
        expose:
            - '10201'